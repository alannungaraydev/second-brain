
## Basic Concepts

Object-oriented programming (OOP) is a paradigm based on wrapping blocks of information and behaviors into batches called objects, which are generated from blueprints defined by a programmer called classes.

Objects, Classes

A class is a design that indicates the characteristics and behaviors that objects instantiated (generated) from them should have
